<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DoSoptTemplate" parent="Theme.Material3.Light.NoActionBar">
        <item name="bottomNavigationStyle">@style/Theme.bottomNavigationViewStyle</item>
    </style>

    <style name="Theme.DoSoptTemplate" parent="Base.Theme.DoSoptTemplate" />

    <style name="Theme.bottomNavigationViewStyle" parent="Widget.Material3.BottomNavigationView">
        <item name="itemIconTint">@color/white</item>
        <item name="itemTextColor">@color/black</item>
        <item name="android:background">@color/gray</item>
    </style>
</resources>